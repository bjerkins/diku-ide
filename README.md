# DIKU IDE 2015

## Setup

Install all the dependencies with `npm`. 

```bash
$ npm install
$ bower install
```

## Run locally

Run the following

```bash
$ npm start
```
which will start a server on `localhost:3000`

